{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "accountantHash",
    "appHash"
  ],
  "properties": {
    "accountantHash": {
      "type": "string",
      "description": "DNA Hash of accountant app which serves registered app",
      "pattern": "^(.*)$"
    },
    "appHash": {
      "type": "string",
      "description": "DNA Hash of registered hosted app",
      "pattern": "^(.*)$"
    }
  }
}